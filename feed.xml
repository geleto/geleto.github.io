<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Angel Popov&#39;s Blog</title>
  <subtitle></subtitle>
  <link href="https://geleto.github.io/feed.xml" rel="self"/>
  <link href="https://geleto.github.io/"/>
  
    <updated>2026-04-30T00:00:00Z</updated>
  
  <id>https://geleto.github.io</id>
  <author>
    <name>Angel Popov</name>
    <email>geleto@gmail.com</email>
  </author>
  
    
    <entry>
      <title>The Kitchen Chef&#39;s Guide to Taming Chaos</title>
      <link href="https://geleto.github.io/posts/cascada-kitchen-chef/"/>
      <updated>2025-10-19T00:00:00Z</updated>
      <id>https://geleto.github.io/posts/cascada-kitchen-chef/</id>
      <content type="html">
        <![CDATA[
      <p>The empty stares I get when I try to explain what I'm working on… well, let's just say I've learned to recognize the precise moment someone mentally taps out, and I don't blame them.</p>
<p>So this article is my attempt to explain something complex in a simple, friendly way - no jargon, no tech-speak, just something intuitive and fun to read.</p>
<p>If you can handle the technical version without mentally checking out, here's the full deep-dive: 👉 <a href="https://geleto.github.io/posts/cascada-script-intro/">geleto.github.io/posts/cascada-script-intro/</a></p>
<p>One of the things I'm working on is the <strong><a href="https://github.com/geleto/cascada/blob/master/docs/cascada/script.md">Cascada language</a></strong> - not a general-purpose language for building apps, but a script your app uses to coordinate and automatically run in parallel lots of interconnected tasks.</p>
<p>Modern software isn't just doing one thing at a time anymore - it might clean up raw data, split it into multiple chunks for parallel processing, send each chunk to an AI to extract insights, use the results to create summaries, and then generate a polished final deliverable - all while, at the same time, sending real-time alerts to users and updating dashboards. Run all of that step-by-step, and suddenly you're waiting minutes instead of seconds.. With Cascada You write simple, in-order, sequential logic, and it automatically runs independent tasks in parallel while guaranteeing every step happens exactly when its input data is ready.</p>
<p>Most people understand cooking better than concurrency, so why not explain Cascada using cooks, ingredients, and a world-class restaurant kitchen that keeps everything running in harmony?</p>
<p>Aprons on - let's step into a kitchen during dinner rush and see how this works...</p>
<hr>
<h2>The Problem: The Overwhelmed Chef</h2>
<img src="/images/posts/chef.png" alt="Overwhelmed chef in a busy kitchen" style="float: left; margin-right: 20px; margin-bottom: 10px; max-width: 200px; border-radius: 8px;">
<p>Imagine a kitchen with a talented Head Chef working alone. He's skilled, but he does only one thing at a time - and it's painfully slow. And it gets worse... modern kitchens (and modern applications) aren't preparing single meals - they're handling <strong>dozens of simultaneous orders</strong>.</p>
<p>He is receiving shipments, checking multiple suppliers, cooking  - all while trying to get food out fast.</p>
<p>It's a mental nightmare. What you really need is a system that can handle massive parallelism while automatically understanding what must wait for what.</p>
<hr>
<h2>The Naive Fix: Hire More Cooks</h2>
<p>So the Chef tries the obvious solution: hire more cooks and divide the work.</p>
<p>An order arrives for a full five-course banquet for twelve guests. The chef assigns each course to a different cook: &quot;Everyone start now - we need this done at the same time!&quot; All five cooks rush to begin simultaneously. It should be beautifully parallel.</p>
<p>Except:</p>
<p>The truffle oil hasn't arrived yet, but Cook A is already frantically searching for it to start the risotto. Cook B fires up twelve steaks at once - three medium-rare, six medium, three well-done. The first few finish perfectly seared, then sit... cooling on the counter, losing their heat and their magic... while he waits for the last well-done steaks to catch up. And because everyone started at the exact same moment, all five cooks need the oven at precisely 6:15pm.</p>
<p>They stand there, waiting their turn. The oven becomes a bottleneck.</p>
<p>Meanwhile, the salad cook finishes in 8 minutes but sits idle for the next 30 while waiting for the entrées. The dessert cook started making soufflés immediately... which collapse by the time the main course is finally ready.</p>
<p>The problem? <strong>Dividing the work</strong> seems parallel, but it ignores reality - what the kitchen needs isn't just dividing work among cooks - it needs <strong>intelligent orchestration</strong> that starts each task exactly when its ingredients are ready and naturally spreads all available work.</p>
<hr>
<h2>The Solution: The Cascada Kitchen Manager</h2>
<img src="/images/posts/robot.png" alt="Hard-working robot manager in a busy kitchen" style="float: left; margin-right: 20px; margin-bottom: 10px; max-width: 200px; border-radius: 8px;">
<p>Now, imagine a completely different kitchen. Here, the Head Chef's job is not to manage, but to <strong>design</strong>.</p>
<p>He sits down in a quiet office and writes out the entire, complex banquet recipe. And here is the most crucial part: <strong>he writes it just as he always has, in a simple, sequential, top-to-bottom list of steps. It looks and feels like any other recipe he's ever written.</strong></p>
<p>But this simple-looking recipe is incredibly powerful because <strong>it's a smart, adaptable plan</strong>. The Chef can include rules, choices, and logic as part of his normal, sequential writing:</p>
<ul>
<li>&quot;<strong>If</strong> the season is Winter, use the root vegetables. <strong>Otherwise</strong>, use the summer squash.&quot;</li>
<li>&quot;<strong>For each</strong> of the 12 dinner orders for the main party, prepare one beef medallion to the customer's requested temperature.&quot;</li>
</ul>
<p>The recipe isn't a rigid checklist - it's a flexible blueprint that responds to real-world conditions.</p>
<p>Once this smart, yet familiar-looking recipe is written, his main job is done. He hands it to a brilliant <strong>Kitchen Manager</strong> (that's <a href="https://github.com/geleto/cascada/blob/master/docs/cascada/script.md">Cascada</a>) and can go relax.</p>
<p>The Manager doesn't just read a fixed list. He <strong>interprets this smart recipe in real-time, as things are happening.</strong> He checks the calendar, sees the 12 orders, understands the client's needs, and then executes the Chef's simple, sequential logic with maximum parallelism, handling all the complexity himself.</p>
<hr>
<h2>Understanding Ingredient Dependencies</h2>
<img src="/images/posts/dependencies.png" alt="Ingredient dependencies chart" style="float: left; margin-right: 20px; margin-bottom: 10px; max-width: 200px; border-radius: 8px;">
<p>The Kitchen Manager is smart enough to handle dependencies within this dynamic logic automatically. Imagine the recipe calls for a final pan sauce, which requires three things: the pan the beef was seared in, some sautéed shallots, and the reduced red wine.</p>
<p>He orchestrates a multi-part ballet:</p>
<ul>
<li>He has Assistant D searing the beef.</li>
<li>Simultaneously, he has Assistant E sautéing shallots.</li>
<li>And at the same time, Assistant F is reducing the wine.</li>
<li>He then tells a final assistant, Assistant G: &quot;Stand by. You cannot start your job until the beef is done, the shallots are ready, AND the wine is reduced. The moment all three are finished - no matter the timing - you will combine them to create the final sauce.&quot;</li>
</ul>
<p>While Assistant G is waiting for these multiple inputs, the rest of the kitchen is not waiting with him! Other dishes are being prepped, other sauces are being reduced. This automatic waiting - without blocking anything else - is the fundamental principle that makes the system so efficient.</p>
<p>The Chef never wrote &quot;wait for the beef&quot; or &quot;check if the shallots are ready.&quot; He just wrote the recipe in order. The Manager figured out the dependencies automatically.</p>
<p>And here's the quiet magic: even though dishes are prepped in wildly unpredictable order - sauces finishing before proteins, garnishes before mains - every plate that leaves the kitchen comes out exactly as the Chef wrote it. From the dining room, it looks as if one unhurried chef had cooked the whole banquet alone.</p>
<hr>
<h2>When Order Matters (!)</h2>
<p>In our kitchen, dozens of things happen at once - rice simmering, sauces reducing, plates going out.
But sometimes, one particular part of a dish must follow a very specific sequence.
That's what the <code>!</code> mark is for.</p>
<p>It tells the Kitchen Manager:
<strong>&quot;For this dish or this component, follow these steps in order - no reordering, no overlap - while everything else runs freely in parallel.&quot;</strong></p>
<p>For example:</p>
<ul>
<li><code>Risotto! Toast the rice in olive oil until translucent.</code></li>
<li><code>Risotto! Add a ladle of stock and stir until absorbed.</code></li>
<li><code>Risotto! Repeat until the rice is just al dente.</code></li>
<li>(Meanwhile, other cooks are grilling steaks, prepping salads, and making desserts...)</li>
<li><code>Risotto.sauce! Reduce the white wine base.</code></li>
<li><code>Risotto.sauce! Whisk in butter, then finish with grated parmesan.</code></li>
<li><code>Risotto! Combine the sauce and rice, season, and plate.</code></li>
</ul>
<p>Whenever the Manager sees a <code>!</code> on <strong>risotto</strong>, they assign one cook to perform those risotto steps in the exact written order.
When they see <code>risotto.sauce!</code>, another cook handles those sauce steps sequentially - <strong>only</strong> the sauce follows that order, not the entire dish.</p>
<p>Think of <code>Risotto!</code> and <code>Risotto.sauce!</code> as two separate assembly lines - each runs its own steps in order, but the two lines operate independently and in parallel with each other. Everything else in the kitchen continues running independently as well.</p>
<p>The <code>!</code> doesn't make the kitchen slower - it simply ensures that for that component, <strong>each step happens exactly in sequence</strong>, while the rest of the kitchen stays fully concurrent.</p>
<p>The final combining step automatically waits for both the rice sequence and the sauce sequence to complete - that's the Manager's automatic dependency tracking at work.</p>
<p><em>(And if the recipe ever went deeper, say <code>risotto.sauce.topping!</code>, the same idea would apply - each level keeps its own internal order without blocking anything else.)</em></p>
<hr>
<h2>The Domino Effect: Handling Kitchen Disasters</h2>
<p>A perfect recipe assumes a perfect kitchen, but in reality, things go wrong. An oven breaks. A key ingredient is missing. An assistant calls in sick.</p>
<p>Traditional kitchens panic - the whole operation can grind to a halt. But the <a href="https://github.com/geleto/cascada/blob/master/docs/cascada/script.md">Cascada</a> Manager handles disasters with what's called <strong><code>error propagation</code></strong> - a controlled ripple effect that contains problems without stopping the entire kitchen.</p>
<p>Imagine someone discovers the truffles that arrived this morning are actually expired and can't be used. The Manager immediately marks <strong>Truffles</strong> as &quot;contaminated&quot; - or in <a href="https://github.com/geleto/cascada/blob/master/docs/cascada/script.md">Cascada</a> terms, &quot;poisoned&quot; (we use this word figuratively to describe how one bad piece of data makes everything depending on it unusable too). This contamination then flows through the dependency chain: the <strong>Truffle Risotto</strong>, which needs those truffles, automatically becomes &quot;unavailable&quot; as well.</p>
<p>Crucially, the assistants making the steak and salad are unaffected. They never needed truffles, so they continue working at full speed. The problem is perfectly contained.</p>
<h3>Three Ways to Handle Disasters</h3>
<p>The Head Chef has powerful options for dealing with unavailable items:</p>
<p><strong>1. Intervene Immediately:</strong> The moment the truffles become unavailable, a contingency plan can trigger automatically: <em>&quot;Send an assistant to the specialty market to buy more!&quot;</em> If they succeed, the problem is solved and the risotto is back on track. This is perfect for retrying failed operations.</p>
<p><strong>2. Substitute Gracefully:</strong> The recipe itself can have a backup plan built in: <em>&quot;If fresh truffles are unavailable, use high-quality mushroom duxelles instead.&quot;</em> The Manager sees this, makes the substitution, and the problem is stopped before it spreads.</p>
<p><strong>3. Check and Adapt Later:</strong> You can also do nothing immediately. The risotto simply remains &quot;unavailable.&quot; Later, you can check its status and decide how to proceed. Maybe you offer the customer an alternative dish. Or perhaps the truffles finally arrive from a backup supplier - in which case you can restart the risotto preparation with the fresh ingredients. The choice of if, when and how to handle the problem is always yours.</p>
<p>The beauty is that unavailability doesn't cause crashes or confusion - it's just information that flows through the system, letting you respond at the right moment.</p>
<hr>
<h2>The Takeaway: Effortless Parallelism</h2>
<p>You write as if every step were a single, simple action. Yet beneath the surface, the system runs a perfectly choreographed symphony of parallel work.</p>
<p>That's the quiet power of being <em>implicitly parallel by design.</em></p>
<h2>Learn More and Try It Yourself</h2>
<p>🚀 <a href="https://geleto.github.io/posts/cascada-script-intro/">CascadaScript Introduction</a> - A more technical introduction to CascadaScript's syntax, features, and how it solves real async programming challenges</p>
<p>📖 <a href="https://github.com/geleto/cascada/blob/master/docs/cascada/script.md">CascadaScript Documentation</a></p>
<p>🧩 <a href="https://github.com/geleto/cascada">Cascada GitHub Repository</a></p>
<p>🤖 <a href="https://github.com/geleto/casai">Casai</a> - an AI orchestration library built on Cascada for effortless, parallel agent and workflow design.</p>
<p>⚡ <strong>Try it now:</strong></p>
<pre class="language-bash"><code class="language-bash"><span class="token function">npm</span> <span class="token function">install</span> cascada-engine</code></pre>
<hr>

    ]]>
      </content>
    </entry>
  
    
    <entry>
      <title>semachunk: Minimal Semantic Chunker for RAG</title>
      <link href="https://geleto.github.io/posts/semachunk-semantic-chunking/"/>
      <updated>2025-11-27T00:00:00Z</updated>
      <id>https://geleto.github.io/posts/semachunk-semantic-chunking/</id>
      <content type="html">
        <![CDATA[
      <p>When building the <a href="https://github.com/geleto/casai-examples">examples</a> for <a href="https://github.com/geleto/casai">Casai</a> - my AI workflow framework - I wanted to include a proper RAG (Retrieval-Augmented Generation) example. Not a toy demo, but something that showed semantic chunking, vector search, and agentic filtering working together.</p>
<p>I hit a wall almost immediately: I couldn't find a TypeScript semantic chunker that fit my needs.</p>
<h2>The Problem</h2>
<p>Most semantic chunking libraries fall into one of three camps:</p>
<ol>
<li>
<p><strong>Heavy solutions</strong> that bundle their own embedding models, require downloads, spin up services, come with big frameworks and pull in massive dependencies</p>
</li>
<li>
<p><strong>Not API/provider agnostic</strong>  -  tightly coupled to a specific embedding provider or API, instead of letting you plug in your own embedding API of choice</p>
</li>
<li>
<p><strong>Fixed-size chunkers</strong> that just split on character count or tokens - missing the whole point of <em>semantic</em> chunking</p>
</li>
<li>
<p><strong>No batch embedding</strong>  -  they merge chunks sequentially, firing off a new embedding request after every single merge. Fine for local models, but hostile to API rate limits.</p>
</li>
</ol>
<p>I needed something different. For an examples repo, I wanted:</p>
<ul>
<li><strong>Zero infrastructure</strong>  -  no containers, no model downloads, no services</li>
<li><strong>Model agnostic</strong>  -  let users plug in whatever embedding provider they're already using (OpenAI, Anthropic, Google, local models, whatever)</li>
<li><strong>Batch-friendly</strong>  -  work <em>with</em> API rate limits, not against them</li>
<li><strong>Simple API</strong>  -  a single function call, not a framework</li>
</ul>
<h2>The Solution: semachunk</h2>
<p>I used <a href="https://github.com/jparkerweb/semantic-chunking">semantic-chunking</a> as a base - it has solid chunking implementation - and built <a href="https://github.com/geleto/semachunk">semachunk</a>, wrapping it in a callback-based API, and replacing the core merging algorithm with a new one that's optimized for batch embeddings, and is both more efficient and should produce better results (instead of merging linearly, it scores all adjacent chunk pairs, selects the best candidates, merges them in one pass, then batch re-embeds).</p>
<pre class="language-typescript"><code class="language-typescript"><span class="token keyword">import</span> <span class="token punctuation">{</span> chunkText <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">'semachunk'</span><span class="token punctuation">;</span>

<span class="token comment">// Chunk the document semantically</span>
<span class="token keyword">const</span> chunks <span class="token operator">=</span> <span class="token keyword">await</span> <span class="token function">chunkText</span><span class="token punctuation">(</span>document<span class="token punctuation">,</span> <span class="token keyword">async</span> <span class="token punctuation">(</span>texts<span class="token punctuation">)</span> <span class="token operator">=></span> <span class="token punctuation">{</span>
    <span class="token comment">// Your embedding logic - any provider, any model</span>
    <span class="token keyword">const</span> response <span class="token operator">=</span> <span class="token keyword">await</span> openai<span class="token punctuation">.</span>embeddings<span class="token punctuation">.</span><span class="token function">create</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
        input<span class="token operator">:</span> texts<span class="token punctuation">,</span>
        model<span class="token operator">:</span> <span class="token string">"text-embedding-3-small"</span>
    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
    <span class="token keyword">return</span> response<span class="token punctuation">.</span>data<span class="token punctuation">.</span><span class="token function">map</span><span class="token punctuation">(</span>d <span class="token operator">=></span> d<span class="token punctuation">.</span>embedding<span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
    maxChunkSize<span class="token operator">:</span> <span class="token number">500</span><span class="token punctuation">,</span>
    similarityThreshold<span class="token operator">:</span> <span class="token number">0.5</span><span class="token punctuation">,</span>
    returnEmbedding<span class="token operator">:</span> <span class="token boolean">true</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// Store in vector index</span>
<span class="token keyword">for</span> <span class="token punctuation">(</span><span class="token keyword">const</span> chunk <span class="token keyword">of</span> chunks<span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token keyword">await</span> index<span class="token punctuation">.</span><span class="token function">upsertItem</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
        vector<span class="token operator">:</span> chunk<span class="token punctuation">.</span>embedding<span class="token punctuation">,</span>
        metadata<span class="token operator">:</span> <span class="token punctuation">{</span> text<span class="token operator">:</span> chunk<span class="token punctuation">.</span>text <span class="token punctuation">}</span>
    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span></code></pre>
<p>That's it. You provide the text and an embedding function, you get back semantically coherent chunks ready for your vector store. No configuration files, no model downloads, no Docker containers.</p>
<h2>A New Algorithm</h2>
<p>The original semantic-chunking library didn't support batch embedding, and its merging algorithm wasn't structured in a way that made adding it straightforward - it merges chunks linearly, one pair at a time. So I wrote a new algorithm designed from the ground up to work in batches:</p>
<ol>
<li>Score <em>all</em> adjacent chunk pairs by similarity</li>
<li>Select the best merge candidates (configurable percentage)</li>
<li>Merge them in one pass</li>
<li>Batch re-embed all affected chunks</li>
<li>Repeat until no good merges remain</li>
</ol>
<p>This should produce better results (by considering all candidates globally rather than just the next pair) and drastically reduces API calls. Instead of potentially hundreds of single-embedding requests, you get a handful of batch requests.</p>
<p>The chunks preserve semantic coherence - a paragraph about a specific topic stays together rather than getting split mid-thought. This matters when you're retrieving context for an LLM; fragmented chunks lead to fragmented understanding.</p>
<h2>Trade-offs</h2>
<p>To be transparent: I haven't battle-tested this extensively. I built it for the Casai examples, and it works well for that use case. If you're processing millions of documents in production, you'll want to do your own evaluation.</p>
<p>The library is intentionally minimal. Just the core semantic chunking algorithm with a clean API.</p>
<h2>Try It</h2>
<p>Install directly:</p>
<pre class="language-bash"><code class="language-bash"><span class="token function">npm</span> <span class="token function">install</span> semachunk</code></pre>
<p>Or see it in action with the RAG example in <a href="https://github.com/geleto/casai-examples">casai-examples</a>:</p>
<pre class="language-bash"><code class="language-bash"><span class="token function">git</span> clone https://github.com/geleto/casai-examples.git
<span class="token builtin class-name">cd</span> casai-examples
<span class="token function">npm</span> <span class="token function">install</span>
<span class="token function">npm</span> run example <span class="token number">14</span></code></pre>
<p>GitHub: <a href="https://github.com/geleto/semachunk">github.com/geleto/semachunk</a></p>
<p>If you're building RAG pipelines and want semantic chunking without the infrastructure overhead, give it a try. Feedback and contributions welcome.</p>
<p><em>semachunk is derived from <a href="https://github.com/jparkerweb/semantic-chunking">semantic-chunking</a> by jparkerweb. Check out the original if you need local model support.</em></p>

    ]]>
      </content>
    </entry>
  
    
    <entry>
      <title>Everything Everywhere All at Once</title>
      <link href="https://geleto.github.io/posts/everything-everywhere-all-at-once/"/>
      <updated>2026-04-29T00:00:00Z</updated>
      <id>https://geleto.github.io/posts/everything-everywhere-all-at-once/</id>
      <content type="html">
        <![CDATA[
      <p>What if there was a programming language where <strong>everything runs at once, concurrently</strong> - every statement, every part of every expression, every step inside every function call, every iteration of every loop, every task in every imported script - and an operation only <strong>waits when it actually depends</strong> on another's result?</p>
<p>I am building exactly that, and today I'm introducing my <a href="https://github.com/geleto/cascada">project</a>.</p>
<p><strong><a href="https://geleto.github.io/cascada-script/">CascadaScript</a></strong> inverts the traditional model: concurrent by default, sequential by exception. And the most extraordinary thing about it is how ordinary its syntax is - instantly familiar to any JavaScript or Python developer. No special async syntax, no <code>await</code>, no <code>Promise.all</code>, no race conditions. Just write your logic. The runtime figures out what can run concurrently from data dependencies alone.</p>
<p>Despite everything running out of order under the hood - all your arrays, objects, and output come out correctly ordered. Always. The final result is identical to sequential execution, just dramatically faster.</p>
<p>It integrates into TypeScript/JavaScript applications and is particularly well suited for AI and LLM orchestration, data pipelines, ETL workflows, and high-throughput I/O coordination. I've also been building <a href="https://github.com/geleto/casai">Casai</a> - an AI orchestration library on top of it.</p>
<p>If you're curious:</p>
<ul>
<li>⚡ <a href="https://geleto.github.io/posts/cascada-script-intro/">CascadaScript Introduction</a> - the technical introduction article for developers</li>
<li>🍳 <a href="https://geleto.github.io/posts/cascada-kitchen-chef/">The Kitchen Chef's Guide to Taming Chaos</a> - the intuitive, no-jargon explanation that everyone can understand</li>
<li>📖 <a href="https://geleto.github.io/cascada-script/">CascadaScript Specification</a> - the full language reference</li>
<li>🧩 <a href="https://github.com/geleto/cascada">Cascada GitHub Repository</a> - open source · <code>npm install cascada-engine</code></li>
</ul>
<p>Feedback very welcome - it's early and it's real.</p>
<p><strong>Think Sequential. Run Concurrently.</strong></p>

    ]]>
      </content>
    </entry>
  
    
    <entry>
      <title>Introduction to CascadaScript: Concurrent by Default, Sequential by Exception</title>
      <link href="https://geleto.github.io/posts/cascada-script-intro/"/>
      <updated>2026-04-30T00:00:00Z</updated>
      <id>https://geleto.github.io/posts/cascada-script-intro/</id>
      <content type="html">
        <![CDATA[
      <h3>CascadaScript - Everything Runs at Once</h3>
<p>CascadaScript is a scripting language where <strong>everything runs at once, concurrently</strong> - every statement, every part of every expression, every step inside every function call, every iteration of every loop, every task in every imported script - and an operation only <strong>waits when it actually depends</strong> on another's result. And the final result is identical to sequential execution - just dramatically faster.</p>
<p>It is a specialized scripting language designed for orchestrating complex asynchronous workflows in JavaScript and TypeScript applications. It is not a general-purpose programming language - instead, it acts as a <strong>data-orchestration</strong> layer for coordinating APIs, databases, LLMs, and other I/O-bound operations with maximum concurrency and minimal boilerplate.</p>
<h4><strong>Ordinary Syntax, Extraordinary Power</strong></h4>
<p>What makes it extraordinary is how ordinary the syntax looks: instantly familiar to any JavaScript or Python developer. It uses familiar syntax and language constructs while offering language-level support for boilerplate-free concurrent workflows, explicit control over side effects, deterministic output construction, and dataflow-based error handling with recovery rollbacks.</p>
<p>Cascada is particularly well suited for:</p>
<ul>
<li><strong>AI, agents, and LLM orchestration</strong> - chain prompts, tools, and retrieval calls without juggling promises (see <a href="https://github.com/geleto/casai">Casai</a>, an AI orchestration library built on Cascada)</li>
<li><strong>Data pipelines</strong> - coordinate parallel stages while keeping the final result deterministic</li>
<li><strong>ETL workflows</strong> - write complex extraction, transformation, validation, enrichment, and loading flows in ordinary code that runs concurrently</li>
<li><strong>High-throughput I/O coordination</strong> - squeeze maximum concurrency out of API-heavy workloads</li>
</ul>
<p>Cascada is open source - explore the project on <a href="https://github.com/geleto/cascada">GitHub</a>.</p>
<hr>
<h2>Quick Links</h2>
<ul>
<li>📖 <a href="https://geleto.github.io/cascada-script/">CascadaScript Specification</a> - the full language reference</li>
<li>🍳 <a href="https://geleto.github.io/posts/cascada-kitchen-chef/">The Kitchen Chef's Guide to Taming Chaos</a> - the intuitive, no-jargon explanation that everyone can understand</li>
<li>🧩 <a href="https://github.com/geleto/cascada">Cascada GitHub Repository</a> - open source · <code>npm install cascada-engine</code></li>
<li>🤖 <a href="https://github.com/geleto/casai">Casai</a> - an AI orchestration library built on Cascada for effortless, concurrent agent and workflow design.</li>
<li>🧪 <a href="https://github.com/geleto/casai-examples">Casai Examples</a> - a collection of practical examples demonstrating AI agent patterns using Casai.</li>
</ul>
<hr>
<h3>The Nightmare: Why Async Programming Is So Hard</h3>
<p><code>async/await</code> is a huge improvement over callback hell. But the moment you want performance, you're back to being a concurrency expert.</p>
<p><strong>The fundamental problem: you manually orchestrate what runs when.</strong> Which operations can run together? Which must wait? You figure it out yourself, every time.</p>
<h4><strong>Shared State</strong></h4>
<p>All operations read and write to shared variables or objects. The complexity explodes:</p>
<ul>
<li>
<p><strong>Manual dependency tracking.</strong> Should operation A <code>await</code> operation B before updating state? Which operations can safely run concurrently? You trace through every possible execution order in your head.</p>
</li>
<li>
<p><strong>Get the order wrong: race conditions.</strong> Operation A reads a value, operation B modifies it, operation A writes based on the stale value. Your state is corrupted.</p>
</li>
<li>
<p><strong>Over-serialize to be safe?</strong> Make everything <code>await</code> everything else to avoid conflicts. Now your concurrent code runs sequentially anyway.</p>
</li>
<li>
<p><strong>Changes ripple everywhere.</strong> Add one new operation that touches shared state? Review every other operation to ensure no new conflicts. Your code becomes brittle and impossible to refactor.</p>
</li>
</ul>
<h4><strong>State Machines &amp; Batch Synchronization</strong></h4>
<p>To avoid this complexity, most frameworks use state machines (with chokepoints between states) or batch synchronization (concurrent tasks that must all complete). Simpler to reason about, but: 19 API calls finish in 50ms, one takes 3 seconds. Everything waits at the chokepoint for the slowest operation-and you still manually coordinate the convergence.</p>
<p><strong>The payoff for getting this right is enormous</strong>-optimal orchestration can cut response times in half or more. But the manual complexity? That's where code breaks.</p>
<p>What if you could skip the manual orchestration entirely? That's exactly what <strong><a href="https://geleto.github.io/cascada-script/">CascadaScript</a></strong> does.</p>
<p>Let's dive into how it flips the script on async programming.</p>
<hr>
<h2>1. ⚡ Concurrent by Default</h2>
<p>The most fundamental shift in Cascada is that it's <strong>concurrent by default</strong>. In most languages, code runs line by line, one after the other. In Cascada, any independent lines of code run at the same time.</p>
<p>Think about fetching a user's profile, their preferences, and their analytics. These are three separate operations that don't depend on each other. In traditional JavaScript, you'd need <code>Promise.all</code> to run them concurrently. Cascada does it automatically.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// These three operations start at the same time, automatically!</span>
<span class="token keyword">var</span> user <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>
<span class="token keyword">var</span> preferences <span class="token operator">=</span> <span class="token function">getUserPreferences</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>
<span class="token keyword">var</span> analytics <span class="token operator">=</span> <span class="token function">getAnalytics</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>

<span class="token comment">// `return` produces the script's output</span>
<span class="token keyword">return</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">user</span><span class="token operator">:</span> user<span class="token punctuation">,</span>
  <span class="token literal-property property">preferences</span><span class="token operator">:</span> preferences<span class="token punctuation">,</span>
  <span class="token literal-property property">analytics</span><span class="token operator">:</span> analytics
<span class="token punctuation">}</span></code></pre>
<p>No <code>Promise.all</code>, no <code>await</code>, no special syntax. If it <em>can</em> run concurrently, it <em>will</em>.</p>
<hr>
<h2>2. 🚦 Data-Driven Flow: Code Runs When Its Inputs Are Ready</h2>
<p>You might be thinking, &quot;What if one operation <em>does</em> depend on another?&quot; Cascada has you covered.</p>
<p>The engine automatically analyzes the data dependencies in your script. An operation will only run once all the variables it needs are ready. This simple rule guarantees the correct order of execution and <strong>completely eliminates race conditions by design</strong>.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// 1. This runs first</span>
<span class="token keyword">var</span> user <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>

<span class="token comment">// 2. This depends on 'user', so Cascada waits for it to resolve</span>
<span class="token keyword">var</span> greeting <span class="token operator">=</span> <span class="token string">"Hello, "</span> <span class="token operator">+</span> user<span class="token punctuation">.</span>name

<span class="token comment">// 3. Meanwhile, these run concurrently with everything above</span>
<span class="token keyword">var</span> posts <span class="token operator">=</span> <span class="token function">fetchPosts</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>
<span class="token keyword">var</span> comments <span class="token operator">=</span> <span class="token function">fetchComments</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>

<span class="token keyword">return</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">greeting</span><span class="token operator">:</span> greeting<span class="token punctuation">,</span>
  <span class="token literal-property property">posts</span><span class="token operator">:</span> posts<span class="token punctuation">,</span>
  <span class="token literal-property property">comments</span><span class="token operator">:</span> comments
<span class="token punctuation">}</span></code></pre>
<p>Here, <code>greeting</code> won't be calculated until <code>fetchUser(123)</code> is complete and the <code>user</code> variable has a value. But <code>posts</code> and <code>comments</code> start fetching immediately since they don't depend on <code>user</code>.</p>
<p>No more subtle timing bugs that only appear in production. The engine orchestrates everything automatically.</p>
<hr>
<h2>3. ✨ No More <code>await</code>: Just Use Your Values</h2>
<p>This is where the magic really happens. Notice the lack of <code>await</code> in the examples above? In Cascada, you never have to think about whether a variable holds a value or a promise. You just use it.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// Traditional JavaScript: Promise hell</span>
<span class="token keyword">const</span> userPromise <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
<span class="token keyword">const</span> name <span class="token operator">=</span> <span class="token keyword">await</span> userPromise<span class="token punctuation">.</span>name<span class="token punctuation">;</span> <span class="token comment">// Wait, can't do this!</span>
<span class="token keyword">const</span> actualUser <span class="token operator">=</span> <span class="token keyword">await</span> userPromise<span class="token punctuation">;</span>
<span class="token keyword">const</span> name <span class="token operator">=</span> actualUser<span class="token punctuation">.</span>name<span class="token punctuation">;</span> <span class="token comment">// Finally!</span></code></pre>
<p>Cascada makes this completely invisible:</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// Cascada: Just use it</span>
<span class="token keyword">var</span> user <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>

<span class="token keyword">return</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">greeting</span><span class="token operator">:</span> <span class="token string">"Hello, "</span> <span class="token operator">+</span> user<span class="token punctuation">.</span>name<span class="token punctuation">,</span>
  <span class="token literal-property property">email</span><span class="token operator">:</span> user<span class="token punctuation">.</span>email<span class="token punctuation">,</span>
  <span class="token literal-property property">status</span><span class="token operator">:</span> <span class="token string">"active"</span> <span class="token keyword">if</span> user<span class="token punctuation">.</span>isActive <span class="token keyword">else</span> <span class="token string">"inactive"</span>
<span class="token punctuation">}</span></code></pre>
<p>Forget <code>.then()</code> and forget manually tracking promises. Cascada handles the asynchronous state invisibly under the hood. You can pass a &quot;future value&quot; (a promise) into a function or use it in an expression, and it just works.</p>
<p>This lets you focus entirely on your business logic, not the async plumbing.</p>
<hr>
<h2>4. 📋 From Chaos to Order: Channels</h2>
<p>So far we've used a simple <code>return</code> to produce output. But what happens when concurrent code needs to <em>contribute</em> to the output? Imagine fetching three users at once and collecting their names:</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token keyword">var</span> userIds <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">101</span><span class="token punctuation">,</span> <span class="token number">102</span><span class="token punctuation">,</span> <span class="token number">103</span><span class="token punctuation">]</span>
<span class="token comment">// We want all three fetched concurrently - but how do we collect the results in order?</span></code></pre>
<p>In a traditional concurrent loop, you'd push to a shared array and pray the order works out. In Cascada, you reach for a <strong>channel</strong>.</p>
<p>A channel is a declared, write-only buffer. You write to it from anywhere - including concurrent loop iterations - and Cascada guarantees the assembled result is always in source-code order. To read the value out, call <code>.snapshot()</code>.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token keyword">var</span> userIds <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token number">101</span><span class="token punctuation">,</span> <span class="token number">102</span><span class="token punctuation">,</span> <span class="token number">103</span><span class="token punctuation">]</span>

<span class="token comment">// Declare a data channel</span>
data result

<span class="token comment">// All three fetchUserDetails calls run concurrently.</span>
<span class="token comment">// Maybe user 103's data comes back first - that's fine.</span>
<span class="token keyword">for</span> id <span class="token keyword">in</span> userIds
  <span class="token keyword">var</span> details <span class="token operator">=</span> <span class="token function">fetchUserDetails</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>
  result<span class="token punctuation">.</span>users<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span>details<span class="token punctuation">.</span>name<span class="token punctuation">)</span>
endfor

<span class="token comment">// `snapshot()` materializes the channel into a value</span>
<span class="token keyword">return</span> result<span class="token punctuation">.</span><span class="token function">snapshot</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token comment">// { "users": ["Alice", "Bob", "Charlie"] } - always in this order</span></code></pre>
<p>Cascada provides three built-in channel types:</p>
<ul>
<li><strong><code>data</code></strong> - for building structured objects and arrays. Supports <code>.push()</code>, <code>.merge()</code>, property assignment, arithmetic operators, and more. The right tool for shaping JSON output.</li>
<li><strong><code>text</code></strong> - for accumulating strings. Calls like <code>log(&quot;hello&quot;)</code> append text in source-code order, perfect for streaming or building reports.</li>
<li><strong><code>sequence</code></strong> - wraps an external object so reads and calls happen in strict source-code order; ideal for stateful APIs like a database client where order matters. We'll come back to this one in the next section.</li>
</ul>
<pre class="language-javascript"><code class="language-javascript">text log
<span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"Starting import\n"</span><span class="token punctuation">)</span>
<span class="token keyword">for</span> user <span class="token keyword">in</span> users
  <span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"Imported: "</span> <span class="token operator">+</span> user<span class="token punctuation">.</span>name <span class="token operator">+</span> <span class="token string">"\n"</span><span class="token punctuation">)</span>
endfor
<span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"Done."</span><span class="token punctuation">)</span>
<span class="token keyword">return</span> log<span class="token punctuation">.</span><span class="token function">snapshot</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre>
<p>This gives you the best of both worlds: maximum I/O performance from concurrent execution, with the reliability of deterministic, source-order assembly.</p>
<hr>
<h2>5. ➡️ Sequential by Exception</h2>
<p>Of course, sometimes you absolutely need things to happen in a specific order - especially when dealing with operations that have side effects, like writing to a database or making stateful API calls.</p>
<p>For these cases, Cascada provides three surgical tools. Each one sequences only what it touches, leaving the rest of the script free to run concurrently.</p>
<p><strong>The <code>!</code> marker</strong> - enforces strict sequential order on a context object path. Perfect for stateful APIs like database handles or external services.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// `bank.account` is provided by the render context.</span>
<span class="token comment">// 1. This MUST finish first</span>
bank<span class="token punctuation">.</span>account<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">deposit</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">)</span>
<span class="token comment">// 2. This waits for deposit to complete</span>
bank<span class="token punctuation">.</span>account<span class="token punctuation">.</span><span class="token function">getStatus</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token comment">// 3. This waits for getStatus</span>
bank<span class="token punctuation">.</span>account<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">withdraw</span><span class="token punctuation">(</span><span class="token number">50</span><span class="token punctuation">)</span>

<span class="token comment">// Meanwhile, these run concurrently with everything above</span>
<span class="token keyword">var</span> preferences <span class="token operator">=</span> <span class="token function">getUserPreferences</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token keyword">var</span> analytics <span class="token operator">=</span> <span class="token function">fetchAnalytics</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre>
<p><strong>The <code>each</code> loop</strong> - iterates one item at a time. Use it when per-iteration side effects must not overlap.</p>
<pre class="language-javascript"><code class="language-javascript">each id <span class="token keyword">in</span> idsToWrite
  db<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">write</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span>   <span class="token comment">// each write completes before the next starts</span>
endeach</code></pre>
<p><strong>The <code>sequence</code> channel</strong> - wraps an external object so all reads and calls happen in strict source-code order. Use it when the object itself is your ordered interface.</p>
<pre class="language-javascript"><code class="language-javascript">sequence db <span class="token operator">=</span> services<span class="token punctuation">.</span>db
<span class="token keyword">var</span> user <span class="token operator">=</span> db<span class="token punctuation">.</span><span class="token function">getUser</span><span class="token punctuation">(</span><span class="token number">1</span><span class="token punctuation">)</span>
<span class="token keyword">var</span> state <span class="token operator">=</span> db<span class="token punctuation">.</span>connectionState</code></pre>
<p>It's <strong>concurrent by default, sequential by exception</strong> - the opposite of traditional programming. You only pay for ordering where you actually need it.</p>
<hr>
<h2>6. ☣️ Errors Flow Like Data</h2>
<p>Traditional <code>try/catch</code> blocks don't work well in a massively concurrent system. If one of fifty concurrent API calls fails, should everything stop?</p>
<p>Cascada uses a more resilient model called <strong>dataflow poisoning</strong>. When an operation fails, it doesn't throw an exception; it produces an <code>Error Value</code>. This error then &quot;poisons&quot; any other variable or operation that depends on it. Crucially, unrelated operations continue running completely unaffected.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// Three concurrent calls - fetchPosts() fails, the others succeed</span>
<span class="token keyword">var</span> user <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>         <span class="token comment">// ✅ Succeeds</span>
<span class="token keyword">var</span> posts <span class="token operator">=</span> <span class="token function">fetchPosts</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>       <span class="token comment">// ❌ Fails and becomes an Error Value</span>
<span class="token keyword">var</span> comments <span class="token operator">=</span> <span class="token function">fetchComments</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span> <span class="token comment">// ✅ Succeeds</span>

<span class="token comment">// Now, let's see what happens:</span>
<span class="token keyword">var</span> userName <span class="token operator">=</span> user<span class="token punctuation">.</span>name           <span class="token comment">// ✅ Works fine, uses the successful result</span>
<span class="token keyword">var</span> commentCount <span class="token operator">=</span> comments<span class="token punctuation">.</span>length <span class="token comment">// ✅ Works fine - unrelated to posts</span>
<span class="token keyword">var</span> postCount <span class="token operator">=</span> posts<span class="token punctuation">.</span>length       <span class="token comment">// ❌ Becomes an error because 'posts' is poisoned</span>

<span class="token comment">// Detect with `is error` and provide fallbacks</span>
<span class="token keyword">if</span> posts is error
  posts <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>           <span class="token comment">// Assign a default</span>
  postCount <span class="token operator">=</span> <span class="token number">0</span>
endif

<span class="token keyword">return</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">userName</span><span class="token operator">:</span> userName<span class="token punctuation">,</span>
  <span class="token literal-property property">postCount</span><span class="token operator">:</span> postCount<span class="token punctuation">,</span>
  <span class="token literal-property property">commentCount</span><span class="token operator">:</span> commentCount
<span class="token punctuation">}</span></code></pre>
<p><strong>Poisoning extends to control flow, too.</strong> If the condition of an <code>if</code> or <code>switch</code> is an Error Value, neither branch runs - and any variable that <em>would have been</em> modified in either branch becomes poisoned. The same applies to loops: a poisoned iterable poisons everything the loop body would have touched.</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token keyword">var</span> role <span class="token operator">=</span> <span class="token string">"guest"</span>
<span class="token keyword">var</span> user <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span><span class="token number">123</span><span class="token punctuation">)</span>  <span class="token comment">// ❌ Fails</span>

<span class="token comment">// `user.isAdmin` is poisoned, so neither branch executes</span>
<span class="token keyword">if</span> user<span class="token punctuation">.</span>isAdmin
  role <span class="token operator">=</span> <span class="token string">"admin"</span>
<span class="token keyword">else</span>
  role <span class="token operator">=</span> <span class="token string">"member"</span>
endif

<span class="token comment">// `role` is now poisoned - both branches would have written to it,</span>
<span class="token comment">// so the engine can't safely leave it as "guest"</span></code></pre>
<p>This ensures corrupted state can't silently slip past as a stale default value. If something would have been touched, it carries the failure forward.</p>
<p>You can also peek inside an Error Value using the <strong><code>#</code> operator</strong> to inspect its details - message, source, and more - without triggering further propagation:</p>
<pre class="language-javascript"><code class="language-javascript"><span class="token keyword">if</span> posts is error
  <span class="token keyword">var</span> msg <span class="token operator">=</span> posts#message
  <span class="token keyword">var</span> origin <span class="token operator">=</span> posts#source<span class="token punctuation">.</span>origin
endif</code></pre>
<p>This approach isolates failures, prevents corrupted data from producing incorrect results, and makes your workflows incredibly robust. The beauty of dataflow poisoning is that you have <strong>complete control over recovery</strong>: detect errors anywhere with <code>is error</code>, repair them with fallbacks, log them for monitoring, or retry - all while the rest of your script continues executing normally.</p>
<hr>
<h2>7. 🛡️ Transactional Recovery with <code>guard</code></h2>
<p>For higher-stakes operations - like database writes that span multiple steps - a single fallback isn't enough. You need to roll back. The <code>guard</code> block is Cascada's transactional recovery primitive: it acts like a save point. If anything inside fails, the channel writes and sequential paths from before the guard are automatically restored.</p>
<pre class="language-javascript"><code class="language-javascript">data out

guard
  out<span class="token punctuation">.</span>status <span class="token operator">=</span> <span class="token string">"processing"</span>

  db<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">beginTransaction</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
  db<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">insert</span><span class="token punctuation">(</span>user<span class="token punctuation">)</span>
  db<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">update</span><span class="token punctuation">(</span>account<span class="token punctuation">)</span>   <span class="token comment">// ❌ Suppose this fails</span>
  db<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">commit</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
  out<span class="token punctuation">.</span>status <span class="token operator">=</span> <span class="token string">"success"</span>

recover err
  <span class="token comment">// STATE RESTORED automatically:</span>
  <span class="token comment">//  - data writes inside the guard are reverted</span>
  <span class="token comment">//  - the db! sequential path is repaired and safe to use again</span>
  db<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">rollback</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
  out<span class="token punctuation">.</span>error <span class="token operator">=</span> <span class="token string">"Transaction failed: "</span> <span class="token operator">+</span> err#message
endguard

<span class="token keyword">return</span> out<span class="token punctuation">.</span><span class="token function">snapshot</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre>
<p>By default, <code>guard</code> protects all channels and sequential paths in scope. The <code>recover</code> block runs only if the guarded code finished in an error state, with the cleanup state already in place. It's like a database transaction - except for your entire workflow.</p>
<hr>
<h2>8. 💡 Familiar Syntax</h2>
<p>Cascada offers a clean, expressive syntax that will feel instantly familiar if you know JavaScript. Variables, conditionals, loops, and reusable functions all work the way you'd expect.</p>
<table>
<thead>
<tr>
<th>Feature</th>
<th>Syntax</th>
</tr>
</thead>
<tbody>
<tr>
<td>Variables</td>
<td><code>var name = value</code></td>
</tr>
<tr>
<td>Conditionals</td>
<td><code>if / elif / else / endif</code></td>
</tr>
<tr>
<td>Switch</td>
<td><code>switch / case / default / endswitch</code></td>
</tr>
<tr>
<td>Logic operators</td>
<td><code>and</code>, <code>or</code>, <code>not</code> (Python-style)</td>
</tr>
<tr>
<td>Concurrent loop</td>
<td><code>for item in array</code></td>
</tr>
<tr>
<td>Sequential loop</td>
<td><code>each item in array</code></td>
</tr>
<tr>
<td>While loop</td>
<td><code>while condition / endwhile</code></td>
</tr>
<tr>
<td>Inline <code>if</code> (ternary)</td>
<td><code>a if condition else b</code> (Python-style)</td>
</tr>
<tr>
<td>Functions</td>
<td><code>function name(x, y=&quot;default&quot;) ... endfunction</code></td>
</tr>
<tr>
<td>Filters</td>
<td><code>value | filterName(args)</code></td>
</tr>
<tr>
<td>Imports</td>
<td><code>import &quot;file&quot; as ns</code></td>
</tr>
<tr>
<td>Comments</td>
<td><code>// line</code>, <code>/* block */</code></td>
</tr>
</tbody>
</table>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// Variables, conditionals, loops</span>
<span class="token keyword">var</span> discount <span class="token operator">=</span> <span class="token number">0</span>

<span class="token keyword">if</span> userType <span class="token operator">==</span> <span class="token string">"premium"</span>
  discount <span class="token operator">=</span> <span class="token number">0.10</span>
endif

<span class="token comment">// Reusable functions with default and keyword arguments</span>
<span class="token keyword">function</span> <span class="token function">formatPrice</span><span class="token punctuation">(</span><span class="token parameter">amount<span class="token punctuation">,</span> discount<span class="token operator">=</span><span class="token number">0</span></span><span class="token punctuation">)</span>
  <span class="token keyword">var</span> final <span class="token operator">=</span> amount <span class="token operator">*</span> <span class="token punctuation">(</span><span class="token number">1</span> <span class="token operator">-</span> discount<span class="token punctuation">)</span>
  <span class="token keyword">return</span> <span class="token string">"$"</span> <span class="token operator">+</span> final
endfunction

<span class="token keyword">return</span> <span class="token function">formatPrice</span><span class="token punctuation">(</span><span class="token number">100</span><span class="token punctuation">,</span> discount<span class="token punctuation">)</span>  <span class="token comment">// "$90"</span></code></pre>
<p>You get variables (<code>var</code>), conditionals (<code>if/elif/else</code>), concurrent (<code>for</code>) and sequential (<code>each</code>, <code>while</code>) loops, reusable <code>function</code> definitions, and modular code organization through <code>import</code> and <code>extends</code>.</p>
<hr>
<h2>🚀 Putting It All Together</h2>
<p>Cascada is <code>a work in progress</code> under active development and evolving quickly. Install it with:</p>
<pre class="language-bash"><code class="language-bash"><span class="token function">npm</span> <span class="token function">install</span> cascada-engine</code></pre>
<p>Here's a realistic example: a customer dashboard endpoint that aggregates data from three APIs, enriches each order, recovers from missing recommendations, and logs the access - all from a single Cascada script.</p>
<p><strong>JavaScript context:</strong></p>
<pre class="language-javascript"><code class="language-javascript"><span class="token keyword">import</span> <span class="token punctuation">{</span> AsyncEnvironment <span class="token punctuation">}</span> <span class="token keyword">from</span> <span class="token string">'cascada-engine'</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> env <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">AsyncEnvironment</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> context <span class="token operator">=</span> <span class="token punctuation">{</span>
  <span class="token literal-property property">userId</span><span class="token operator">:</span> <span class="token number">42</span><span class="token punctuation">,</span>

  <span class="token comment">// Three independent data sources</span>
  <span class="token function-variable function">fetchUser</span><span class="token operator">:</span>            <span class="token punctuation">(</span><span class="token parameter">id</span><span class="token punctuation">)</span> <span class="token operator">=></span> api<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">/users/</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>id<span class="token interpolation-punctuation punctuation">}</span></span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">,</span>
  <span class="token function-variable function">fetchOrders</span><span class="token operator">:</span>          <span class="token punctuation">(</span><span class="token parameter">id</span><span class="token punctuation">)</span> <span class="token operator">=></span> api<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">/users/</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>id<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">/orders</span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">,</span>
  <span class="token function-variable function">fetchRecommendations</span><span class="token operator">:</span> <span class="token punctuation">(</span><span class="token parameter">id</span><span class="token punctuation">)</span> <span class="token operator">=></span> recommender<span class="token punctuation">.</span><span class="token function">suggestFor</span><span class="token punctuation">(</span>id<span class="token punctuation">)</span><span class="token punctuation">,</span>

  <span class="token comment">// Per-order enrichment - runs concurrently for each order</span>
  <span class="token function-variable function">enrichOrder</span><span class="token operator">:</span> <span class="token punctuation">(</span><span class="token parameter">order</span><span class="token punctuation">)</span> <span class="token operator">=></span> api<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token template-string"><span class="token template-punctuation string">`</span><span class="token string">/orders/</span><span class="token interpolation"><span class="token interpolation-punctuation punctuation">${</span>order<span class="token punctuation">.</span>id<span class="token interpolation-punctuation punctuation">}</span></span><span class="token string">/details</span><span class="token template-punctuation string">`</span></span><span class="token punctuation">)</span><span class="token punctuation">,</span>

  <span class="token comment">// Stateful service for audit logging</span>
  <span class="token literal-property property">audit</span><span class="token operator">:</span> auditLogger<span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">;</span>

<span class="token keyword">const</span> dashboard <span class="token operator">=</span> <span class="token keyword">await</span> env<span class="token punctuation">.</span><span class="token function">renderScriptString</span><span class="token punctuation">(</span>script<span class="token punctuation">,</span> context<span class="token punctuation">)</span><span class="token punctuation">;</span></code></pre>
<p><strong>Cascada script:</strong></p>
<pre class="language-javascript"><code class="language-javascript"><span class="token comment">// Three sources fetched concurrently</span>
<span class="token keyword">var</span> user <span class="token operator">=</span> <span class="token function">fetchUser</span><span class="token punctuation">(</span>userId<span class="token punctuation">)</span>
<span class="token keyword">var</span> orders <span class="token operator">=</span> <span class="token function">fetchOrders</span><span class="token punctuation">(</span>userId<span class="token punctuation">)</span>
<span class="token keyword">var</span> recs <span class="token operator">=</span> <span class="token function">fetchRecommendations</span><span class="token punctuation">(</span>userId<span class="token punctuation">)</span>

data report

<span class="token comment">// Cascada waits for `user` to resolve, then writes both properties</span>
report<span class="token punctuation">.</span>user<span class="token punctuation">.</span>name <span class="token operator">=</span> user<span class="token punctuation">.</span>name
report<span class="token punctuation">.</span>user<span class="token punctuation">.</span>tier <span class="token operator">=</span> <span class="token string">"premium"</span> <span class="token keyword">if</span> user<span class="token punctuation">.</span>lifetimeValue <span class="token operator">></span> <span class="token number">1000</span> <span class="token keyword">else</span> <span class="token string">"standard"</span>

<span class="token comment">// Each order is enriched concurrently - but the final array is in source order</span>
<span class="token keyword">for</span> order <span class="token keyword">in</span> orders
  <span class="token keyword">var</span> details <span class="token operator">=</span> <span class="token function">enrichOrder</span><span class="token punctuation">(</span>order<span class="token punctuation">)</span>
  report<span class="token punctuation">.</span>orders<span class="token punctuation">.</span><span class="token function">push</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
    <span class="token literal-property property">id</span><span class="token operator">:</span> order<span class="token punctuation">.</span>id<span class="token punctuation">,</span>
    <span class="token literal-property property">total</span><span class="token operator">:</span> details<span class="token punctuation">.</span>total<span class="token punctuation">,</span>
    <span class="token literal-property property">status</span><span class="token operator">:</span> details<span class="token punctuation">.</span>status
  <span class="token punctuation">}</span><span class="token punctuation">)</span>
endfor

<span class="token comment">// Recommendations are nice-to-have - recover with an empty list if they fail</span>
<span class="token keyword">if</span> recs is error
  recs <span class="token operator">=</span> <span class="token punctuation">[</span><span class="token punctuation">]</span>
endif
report<span class="token punctuation">.</span>recommendations <span class="token operator">=</span> recs

<span class="token comment">// Audit the access - `!` keeps log calls sequential</span>
<span class="token comment">// without slowing anything else down</span>
audit<span class="token operator">!</span><span class="token punctuation">.</span><span class="token function">log</span><span class="token punctuation">(</span><span class="token string">"dashboard_viewed"</span><span class="token punctuation">,</span> userId<span class="token punctuation">)</span>

<span class="token keyword">return</span> report<span class="token punctuation">.</span><span class="token function">snapshot</span><span class="token punctuation">(</span><span class="token punctuation">)</span></code></pre>
<p>In one short script you've got three concurrent API calls, a concurrent enrichment loop with deterministic ordering, error recovery for non-critical data, and a sequential side effect - without <code>Promise.all</code>, <code>await</code>, or a single race condition to reason about.</p>
<p><strong>⚠️ Heads up!</strong> Cascada is a new project. You might run into bugs, and the documentation is catching up with the code. Your feedback and contributions are welcome as we build the future of asynchronous programming.</p>
<hr>
<h2>🎯 Why This Matters</h2>
<p>Cascada isn't trying to replace JavaScript - it's designed to be the backbone of your data layer.</p>
<p>Use it to compose complex workflows that wire together LLMs, APIs, databases, and external services. By inverting the traditional programming model - concurrent by default, sequential by exception - it lets you build high-performance data pipelines that are surprisingly simple and intuitive, all with maximum I/O throughput and minimum mental overhead.</p>
<p>The result? <strong>Code that reads like synchronous logic but executes with the performance of carefully orchestrated async operations.</strong> You get to focus on <em>what</em> you're building instead of <em>how</em> to manage promises, race conditions, and execution order.</p>
<p>And the best part? When you look at your Cascada script six months later, you'll actually understand what it does.</p>
<hr>
<h2>Final Thoughts</h2>
<p>If you've ever fought through promise hell, callback pyramids, or race conditions, Cascada might just feel like magic.</p>
<p>It turns async programming from a juggling act into a walk in the park. You write code like a human - simple, sequential, intuitive - and let the runtime handle the concurrency and safety behind the scenes.</p>
<p>The future of async programming isn't about getting better at promises. It's about not having to think about them at all.</p>

    ]]>
      </content>
    </entry>
  
</feed>